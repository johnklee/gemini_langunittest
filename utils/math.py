"""Module to conduct math operations."""

def add(a: float, b: float) -> float:
  """Addes `a` with `b`.

  Args:
    a: Value 1 to add.
    b: Value 2 to add.

  Returns:
    `a + b`
  """
  return a + b
